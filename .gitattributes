# Normalise line endings so the repo is consistent regardless of platform —
# this is built on Windows and edited on a Mac.
* text=auto eol=lf

# Binary: never touch these.
*.jpg  binary
*.jpeg binary
*.png  binary
*.webp binary
*.avif binary
*.ico  binary
